// Continue is a Keyword in JS
// Continue is used to skip the current iteration of the loop of the program
for (let i = 1; i <= 10; i++) {
  if (i >= 7) {
    console.log(`I am at ${i}`);
    continue;
  }
}
