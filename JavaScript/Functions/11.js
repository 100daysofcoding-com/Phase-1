function userLogin(name, age, lastname, password) {
  console.log(
    `Your name is ${name} and age is ${age} and lastname is ${lastname} and your password is ${password}`
  );
}

userLogin("Suhail", 20, "roushan", "3784832yhjfdjkf");
