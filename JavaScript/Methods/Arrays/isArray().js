const num = [2, 4, 5, 6, 7];

const num2 = { one: 1, two: 2 };

// isArray()
// Array.isArray(num) ==> True or False
// It checks wheather the given array is array or Not 
// If its an Array ===> True
// If Not then ===> False
let output = Array.isArray(num2);
console.log(output);
