const names = ["Suhail", "Saif", "Izhaan"];
//              0          1         2       3           4

// arrName.length
// To get the no.of elements in an array ..use .length
// arrName.length
let output = names.length;
console.log(output);
console.log(names.length);

// 52
